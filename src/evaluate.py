import mlflow
import pandas as pd
from mlflow.tracking import MlflowClient
import argparse
from preprocess import preprocess

def main():
    """Gets the current model marked as 'Champion', i.e., best model and evaluates """

if __name__ == "__main__":
    main()